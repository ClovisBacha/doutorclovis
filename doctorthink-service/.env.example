# ── Banco PRÓPRIO do DoctorThink (crie um projeto Supabase NOVO) ──────────
# Rode o schema.sql no SQL Editor desse projeto antes de subir.
SUPABASE_URL=""
SUPABASE_SERVICE_ROLE_KEY=""

# ── IA (embeddings) — mesma chave do Google Generative AI ─────────────────
GOOGLE_GENERATIVE_AI_API_KEY=""

# ── Servidor ──────────────────────────────────────────────────────────────
PORT="8787"

# ── Migração (opcional): banco de ORIGEM = Supabase da Obstétrica ─────────
# Usado só por `bun run migrate-from-obstetrica.ts`.
SOURCE_SUPABASE_URL=""
SOURCE_SUPABASE_SERVICE_ROLE_KEY=""
